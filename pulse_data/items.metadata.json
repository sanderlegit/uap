{
    "name": "items",
    "columns": [
        "id",
        "source_id",
        "external_id",
        "title",
        "author",
        "content",
        "url",
        "published_at",
        "collected_at",
        "platform",
        "metadata",
        "content_quality",
        "duplicate_of",
        "cluster_id"
    ],
    "schema": "CREATE TABLE items (\n    id INTEGER PRIMARY KEY AUTOINCREMENT,\n    source_id INTEGER NOT NULL,\n    external_id TEXT NOT NULL,\n    title TEXT,\n    author TEXT,\n    content TEXT,\n    url TEXT,\n    published_at TEXT,\n    collected_at TEXT DEFAULT (datetime('now')),\n    platform TEXT NOT NULL,\n    metadata TEXT DEFAULT '{}', content_quality TEXT DEFAULT 'unknown', duplicate_of INTEGER, cluster_id INTEGER,\n    FOREIGN KEY (source_id) REFERENCES sources(id),\n    UNIQUE(platform, external_id)\n)"
}